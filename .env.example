# ==========================================
# ENV Variables for WebRTC Employee System
# ==========================================

# 1. API Configuration
PROJECT_NAME="Windows Monitoring System API"
PORT=8000
ALLOWED_ORIGINS="https://youradminpanel.com,https://www.youradminpanel.com,http://localhost:3000"

# 2. Database (PostgreSQL)
# Note: Since the DB isn't in docker-compose, provide the remote RDS or self-hosted DB credentials here.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_db_password
POSTGRES_SERVER=your_db_host_ip_or_rds_endpoint
POSTGRES_PORT=5432
POSTGRES_DB=employee_monitoring

# 3. Redis Configuration
# Inside docker-compose we map REDIS_HOST=redis, so these can be left as default for local docker redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# 4. Security Keys (MUST CHANGE IN PROD)
# Generate one using `openssl rand -hex 32`
SECRET_KEY=CHANGE_THIS_SECRET_KEY_IN_PRODUCTION
ALGORITHM=HS256


# ==========================================
# Client & Desktop Settings (For reference when packaging the client)
# ==========================================
# SERVER_URL=https://api.yourdomain.com/api/v1
# WS_URL=wss://api.yourdomain.com/api/v1/ws
